<% if user_signed_in? %>
  <%= form_with model: @post, url: posts_path do |form| %>
    <%= form.text_area :body, class: "border-b", placeholder: "Write here!" %>
    <%= form.submit "Post!", class: button_class(theme: "primary") %>
  <% end %>
<% else %>
  <p> You are not logged in! </p>
<% end %>